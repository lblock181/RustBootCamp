/// tuple structs
/// 
/// helps for definition/enforment of data types & num or elements
/// 

// tuple structs defined by parens w/o any definitions
struct RGB(i32,i32,i32);
struct CMYK(i32,i32,i32,i32);

// unit-like struct
// rarely used
struct MyStruct;

fn main() {
    println!("Hello, world!");
}
